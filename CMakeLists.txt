cmake_minimum_required(VERSION 3.2.2)
project(precol)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14 -fopenmp -lmetis")
#set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -fopenmp  -D_GLIBCXX_PARALLEL")
include_directories(inc/)
include_directories(ordering/)
include_directories(silu/)
include_directories(algs/)
include_directories(src/)
file(GLOB SRC "src/*.cpp")
file(GLOB ALG_SRC "algs/*.cpp")
file(GLOB ORD_SRC "ordering/*.cpp")
file(GLOB ILU_SRC "silu/*.cpp")
add_executable(precol ${SRC} ${ALG_SRC} ${ORD_SRC} ${ILU_SRC})